"use strict"

var digit = "";
digit = prompt("Give me a number:");

var x = numberHappiness(digit);

function numberHappiness(digit);{
	var digit = number.toString();
	var digSum = 0;
	var collectiveNumbers[];

	for (var n=0; n < digit.length; n++){
		var square = math.pow(digit[i], 2);
		digSum += square;
		}
		collectiveNumbers.push(digSum);
		if (digSum === 1){
			document.write("This is a happy number!");
		}
		else if(){
			document.write("This is not a happy number.");
		}
		else {
			numberHappiness(digSum);
		}
	}
 
